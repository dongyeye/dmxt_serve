<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fzu.edu.dmxt.dao.UserDao">
    <!-- 目的：为DAO接口方法提供sql语句配置 -->
    <!-- 查询用户和密码进行登录 -->
    <select id="findByUsername" resultType="User" parameterType="String">

        select * from user where username = #{username}

    </select>
    <insert id="add" parameterType="User" >
        insert into user ( user_name,user_tel,user_avat,user_sex,user_land_time,user_reg_time,user_online,role_id )
        values (#{user_name},#{user_tel},#{user_avat},#{user_sex},#{user_land_time},#{user_reg_time},#{user_online},#{role_id})
    </insert>

    <delete id="delete" parameterType="User" >
        delete from user where user_id= #{user_id}
    </delete>

    <select id="get" parameterType="_int" resultType="User">
        select * from   user  where user_id= #{user_id}
    </select>

    <update id="update" parameterType="User" >
        update user set user_name=#{user_name},user_tel=#{user_tel},user_avat=#{user_avat},user_sex=#{user_sex},
        user_land_time=#{user_land_time},user_reg_time=#{user_reg_time},user_online=#{user_online},role_id=#{role_id}
        where user_id=#{user_id}
    </update>
    <!--<select id="list" resultType="User">-->
        <!--select * from   user-->
    <!--</select>-->

    <select id="list" resultType="User">
        select * from  user  order by user_id desc
        <if test="start!=null and count!=null">
            limit #{start},#{count}
        </if>

    </select>
    <select id="total" resultType="int">
        select count(*) from user
    </select>
</mapper>